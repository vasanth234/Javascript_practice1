/*document.addEventListener('DOMContentLoaded', function() {
    const galleryImages = document.querySelectorAll('.gallery img');
    const popup = document.querySelector('.popup');
    const popupImg = document.querySelector('.popup img');
    const closePopupBtn = document.querySelector('.popup span');
  
    // Loop through each image in the gallery
    galleryImages.forEach(img => {
      // Add click event listener to each image
      img.addEventListener('click', function() {
        // Display the popup
        popup.style.display = 'block';
        // Set the clicked image source as the popup image source
        popupImg.src = this.getAttribute('src');
      });
    });
  
    // Add click event listener to close button in popup
    closePopupBtn.addEventListener('click', function() {
      // Hide the popup when close button is clicked
      popup.style.display = 'none';
    });
  
    // Optionally, close popup when clicking outside of the image (on the overlay)
    popup.addEventListener('click', function(event) {
      if (event.target === this) {
        popup.style.display = 'none';
      }
    });
  });*/

  

  